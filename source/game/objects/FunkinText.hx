/**
 * Same as `UNOSprite` but, yea, u know... ion really have to explain
**/

package game.objects;

import game.objects.UNOText;

class FunkinText extends UNOText {
    public function new(x:Float, y:Float, text:Dynamic, settings:FormatSettings):Void
        super(x, y, text, settings);
}