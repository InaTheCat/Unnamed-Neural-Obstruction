package game;

// class Combo extends FlxSpriteGroup {
    // function new(x:Float, y:Float) {
        // super();

        // setPosition(x ?? 0, y ?? 0);
    // }

    // public function (rating:String, combo:Int) {
        // var rate:FlxSprite = new FlxSprite().loadGraphic(Paths.image('game/combo/' + (rating ?? 'sick')));
        // add(rate);

    // }
// }