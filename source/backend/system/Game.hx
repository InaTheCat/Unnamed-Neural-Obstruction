// package system;

// import flixel.FlxGame;
// import flixel.util.typeLimit.NextState.InitialState;

// class Game extends FlxGame {
    // override public function new(initialState:InitialState) {
        // super(1280, 720, initialState, 120, 120, true, false);
    // }

    // private var consoleOpened:Bool = false;

    // override public function update() {
        // super.update();

        // if (flixel.FlxG.keys.justPressed.F2 && !consoleOpened){
            // system.DesktopAPI.showConsole();

            // consoleOpened = true;
        // }
    // }
// }